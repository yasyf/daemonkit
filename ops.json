{"v":1,"lamport":4,"session":"cfa0f537-f63b-4af9-844c-1efcf2ab6814","ops":[{"kind":"append_entry","text":"launchd exports the Runner seam but no default implementation, so every consumer that needs one duplicates a ~10-line launchctlRunner (captain-hook did during the v0.21 migration; deploy keeps its own unexported execRunner at deploy/deploy.go:616). Either export the default runner or document why callers must own the process boundary — three copies of the same exec wrapper is the smell."}]}
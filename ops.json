{"v":1,"lamport":6,"session":"4a23a643-72bd-4add-8b03-cbe4379168c6","ops":[{"kind":"append_entry","text":"HOME'S SEPARATE CELL CLOSED LIVE (2026-07-26): the desired-set surgery (dkservicefix, Option B) cleared the stale Background-keyed records from services.db, and `synckitd install` on synckit v0.36.3 then bootstrapped the keyless reposync helper through daemonkit's caller over SSH first-try — zero EIO, helper up 6h17m with zero respawns, live RPC traffic, handshake counter flat. That empirically confirms the keyless-emission fix for the Background-key cell named in the root-cause note as \"released and awaits the desired-set recovery unwedge\" — unwedged and field-proven.\n\nThis investigation's remaining arc is solely the LOCAL temp-HOME cell: the fix bundle (internal/realhome passwd-based resolution with DAEMONKIT_HOME test seam + launchctl exit-5 declassification with plist-path-leading diagnostic + reconcileStrict/reconcileRecovery threading) passed adversarial review clean and sits ship-ready uncommitted in the daemonkit worktree (branch real-home-resolution, base 8971ae9). Release is user-gated (freeze exception #2). Closure path unchanged: v0.20.9 ships → captain-hook repins (12.20.12) → local `brew reinstall captain-hook` postinstall converges through the previously-failing brew temp-HOME context → investigation_fix + investigation_confirm."}]}
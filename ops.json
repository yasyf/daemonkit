{"v":1,"lamport":1,"session":"cfa0f537-f63b-4af9-844c-1efcf2ab6814","ops":[{"kind":"create_note","nonce":"59eec9330f4c5c65885383b71512f3ca","title":"Fleet migration trap 4: HOME-based test sandboxing is dead under v0.21 — use DAEMONKIT_HOME","body":"Found by binrun's repin lane (2026-08-03), the fleet's linux canary, but nothing about it is linux-specific. v0.21's paths resolves home through internal/realhome, which reads the passwd database and deliberately ignores HOME (its godoc: \"never the caller's HOME ... so a sandboxed environment cannot relocate state\"). Any test that sandboxes daemonkit-reached state via t.Setenv(\"HOME\", tmp) or a \"HOME=\"+dir subprocess env now reads and writes the REAL ~/.daemonkit tree: fixtures go invisible, artifact falls through to real network downloads, gc prunes nothing.\n\nThe fix: those sites move to DAEMONKIT_HOME (internal/realhome.EnvOverride — the documented override, honored in production with a slog.Warn). Second-order: that warning lands on stderr, so tests asserting clean stderr need the consumer's own log-level knob (binrun: LOG_LEVEL=error in subprocess envs).\n\nNot mechanical: a HOME setenv that sandboxes the repo's OWN os.UserHomeDir paths (which honor HOME) stays. Judge per site: does the code under test reach daemonkit state?\n\nMeasured 2026-08-03 (files with Setenv(\"HOME\") in *_test.go): cc-patch 3, synckit 2, fusekit 1, cc-present 5, cc-orchestrate 3, reposync 2, cc-notes 8 (+26 \"HOME=\" env lines), binrun 1 (fixed). Clean: cc-interact, captain-hook, cc-review. Every consumer lane checks its sites before shipping; this joins the three MIGRATING-0.21.md traps (MaxFrame shrink, markerless plists, Restart zero-value) on the per-repo ship gate.","tags":null,"anchors":[]}]}
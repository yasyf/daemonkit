# signed trust E2E fixtures (regenerate: scripts/e2e-trust.sh)
.trust-fixtures/

# stray binaries from `go build ./internal/<pkg>/` or `./ci/<pkg>/` at the root
# (each ~4 MB, and one `git add -A` from the index)
/exportcensus
/testserver
/trustfixture
/wiregen
